package frc.robot.commands;

public class testStateMachine {
  //set the public enum and name it TEST_STATES
  //then give it two states el3 and el4
  

  //then create a TEST_STATES variable and name it targetState, and give it a default value of el3


  
  //now create a method that returns a a testSTates variable and name it getTargetState, this should me named getTargetState


  //then create a method that takes in a TEST_STATES variable and name it setTestState, this should be named setTargetState
  //and this method will set the target state equal to the state value that is passed in. 
  
  //now we are actually going to write the advanceTargetState method this can be a switch but it can also just be if and else statements
  //first create a switch that takes in the targetState variable and checks el3, if thats true then its going to set the target state to the 
  //test state el4, then we are going to break out of the switch

  //then we are going to check if the target state is el4, if it is then we are going to set the target state to el3
  //then we are going to break out of the switch
  

 
}
